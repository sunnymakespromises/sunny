'use client'

import Window from '../../components/window'

/* /app/donate/blog.js */
export default function Blog() {
    return (
        <Window id = 'blog-window' classNames = 'w-full h-full flex-row' background = 'blur'>

        </Window>
    )
}