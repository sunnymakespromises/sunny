'use client'

/* /app/donate/settings.js */
export default function Settings() {
    return (
        <div id = 'settings-window' className = 'w-full h-full flex flex-col items-center'>
        </div>
    )
}